import React from 'react';

export default function RtcStream({ socket }) {
    React.useEffect(() => {
        //console.log(x);
    }, [socket])

    return (
        <div>
            RtcStream
        </div>
    );
}